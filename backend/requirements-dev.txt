-r requirements.txt
httpx==0.27.2
pytest>=8,<10
# Retained backend research modules and their original tests only.
pandas>=2.2,<3
numpy>=1.26,<3
# Generate real legacy XLS fixtures for file-exchange tests only.
xlwt==1.3.0

# Development-only formatter and static checks.
ruff==0.12.12
